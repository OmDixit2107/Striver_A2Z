#include <bits/stdc++.h>
using namespace std;
void explainSet()
{
    set<int> s;
    s.insert(1);
    auto it = s.find(1);
    s.erase(1);
    // s.erase(it1, it2);
}

int main()
{
    explainSet();
    return 0;
}